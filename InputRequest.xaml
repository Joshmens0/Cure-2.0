<UserControl x:Class="Cure_2._0.InputRequest"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:Cure_2._0"
             mc:Ignorable="d"
             d:DesignHeight="50" d:DesignWidth="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>
        <TextBox x:Name="input" x:FieldModifier="public"
            FontFamily="Segoe UI" FontSize="16"
            VerticalContentAlignment="Center" HorizontalContentAlignment="Left"
            BorderThickness="1" BorderBrush="#CCCCCC" Background="White"
            Foreground="#333333" Margin="5" Padding="10"
            Text="Send A Message" MouseDown="input_MouseDown" GotFocus="input_GotFocus" LostFocus="input_LostFocus" />
        <Button Grid.Column="1" Content="Send" Width="80" Margin="5"
                FontFamily="Segoe UI" FontSize="16"
                Background="#007ACC" Foreground="White"
                BorderThickness="0" Click="SendButton_Click"/>
    </Grid>
</UserControl>

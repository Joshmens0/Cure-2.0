<UserControl x:Class="Cure_2._0.ImageGenerator"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:Cure_2._0"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="10">
            <TextBox x:Name="ImagePrompt" Width="500" Margin="5"
                     FontFamily="Segoe UI" FontSize="16"
                     VerticalContentAlignment="Center" HorizontalContentAlignment="Left"
                     BorderThickness="1" BorderBrush="#CCCCCC" Background="White"
                     Foreground="#333333" Padding="10"
                     Text="Enter a prompt to generate an image"/>
            <Button Content="Generate" Width="100" Margin="5"
                    FontFamily="Segoe UI" FontSize="16"
                    Background="#007ACC" Foreground="White"
                    BorderThickness="0" Click="GenerateButton_Click"/>
        </StackPanel>
        <ProgressBar Grid.Row="1" x:Name="loading" IsIndeterminate="True" Visibility="Collapsed" Height="10" Width="200"/>
        <Image Grid.Row="1" x:Name="GeneratedImage" Margin="10"/>
    </Grid>
</UserControl>

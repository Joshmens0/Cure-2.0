<UserControl x:Class="Cure_2._0.OutputDisplayTemplate"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:Cure_2._0"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="50"/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <StackPanel Grid.Column="0">
            <Image  Height="30" VerticalAlignment="Top" x:Name="ProfileImage" Source="/bot.jpeg" Stretch="Uniform" Margin="10"/>
        </StackPanel>

        <Border Grid.Column="1" Background="#E6F0FF" CornerRadius="5" Margin="10">
            <TextBlock VerticalAlignment="Stretch" x:Name="DisplayResponse"
                TextAlignment="Left" Padding="15" FontSize="16" FontFamily="Segoe UI"
                FontWeight="Normal" TextWrapping="Wrap" Foreground="#333333" />
        </Border>
    </Grid>
</UserControl>
